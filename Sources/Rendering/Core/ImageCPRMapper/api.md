TODO:
- a better example
- read the proposal
- all todos:
    LATER: test for OpenGL Mapper: // TODO
    LATER: test for Core Mapper: // TODO
    LATER: ts header of OpenGL Mapper: // TODO

LATER: quaternions instead of vectors (gimbal lock) + mctc matrix in vertex shader

Rollback package.json: `"prepare": "npm run build:esm && cp -rf ./dist/esm/* ./"`

Use multiple commits:
fix(AbstractMapper3D): reformat vtkAbstractMapper3D
feat(ImageCPRMapper): add ImageCPRMapper
fix(fitProps): invalid bounding box case
fix(fitProps): distance to camera depends on parallelScale
